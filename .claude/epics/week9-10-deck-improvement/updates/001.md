## Coordinator follow-up (applied for real)

The build job hit the harness's worktree-isolation bug and could not execute
any shell command against this worktree. The coordinator located the
scratch handoff bundle it had left behind (inside week7-8's worktree, at
`.claude/tmp/week9-10-deck-improve-handoff/`, itself a symptom of the same
harness pin-drift bug) and copied both corrected deck files and the lecture
markdown verbatim into this worktree:

- `src/content/lectures/week-09.md`
- `src/decks/week-09.deck.mdx`
- `src/decks/week-10.deck.mdx`

`git diff --stat` confirmed only these three files changed, matching this
task's declared scope (background-path fixes plus the two week-10
`![bg ...]` → `<img>` conversions). Status: **completed**.
