## Coordinator follow-up (run for real)

The coordinator entered the worktree directly, applied the scratch content
(see `updates/001.md`, `updates/002.md`), and ran everything this task
specced:

- Playwright walk of both decks (week-09, week-10) at 1280×800 and
  375×812 (`astro dev` on port 4713): zero `pageerror`/`console.error` at
  every slide, both viewports, both decks.
- Discovered a genuine clipping defect not present in the scratch handoff's
  own claims: `meat-thermometer.jpg` on week-10's "Poultry, fish and red
  meat" slide extended to `bottom: 908.6px` at 1280×800 — well past the
  visible canvas — while fine at 375×812. This class of defect (image
  overflow) is not caught by `pnpm check`'s own astromotion deck-check,
  which measures only text-bearing elements for overflow (confirmed by
  reading `node_modules/astromotion/src/deck-check.mjs`), so `pnpm check`
  passed clean throughout despite the defect. Fixed by reducing the image's
  inline style from `max-height: 200px; margin: 10px auto;` to
  `max-height: 80px; margin: 4px auto 0;` — a pure layout change, no prose
  touched. Re-verified: `bottom` now 782.6px at 1280×800 (782.6 < 800,
  clear of the viewport) and 518.1px at 375×812; screenshots confirm no
  clipping at either viewport.
- The `apple`/`bananas` bg-directive images on week-09 correctly return
  `null` for `document.querySelector('img[src*=...]')` — expected, since
  astromotion's `![bg ...]` directive renders as a CSS `background-image`
  on a `div`, never a real `<img>` element (confirmed against
  `remark-deck-bg.ts`). Not a defect.
- Content-duplication cross-check: week-10's reused figures (1–2 days
  poultry, 3–5 days whole cuts, 4–60 °C / 40–140 °F danger zone) match
  week-03 and week-04's own decks exactly — grepped and confirmed
  character-for-character consistent, nothing restated incorrectly.
- `SOURCES.md` cross-check for `src/decks/assets/week-10/`: both
  `meat-thermometer.jpg` and `handwashing.jpg` credit lines present and
  unchanged.
- `pnpm check`: typecheck + production build + axe (48 pages, no
  violations) + broken-links checker (none) + astromotion deck-structure
  check (12 decks, no violations) + vitest (11/11 passed).
- `pnpm check:evidence`: fails only on the pre-existing, unrelated
  PROCESS.md template/citation gate — same failure mode already present
  before this epic, nothing from this epic's scope caused it.
- `git diff --stat`: only `src/content/lectures/week-09.md`,
  `src/decks/week-09.deck.mdx`, and `src/decks/week-10.deck.mdx` changed
  outside `.claude/epics/` — matches the epic's declared scope exactly.

Definition of done: all points satisfied, including the one item the
handoff bundle could not verify (no shell access) and the one real defect
that verification itself caught. Status: **completed**.
