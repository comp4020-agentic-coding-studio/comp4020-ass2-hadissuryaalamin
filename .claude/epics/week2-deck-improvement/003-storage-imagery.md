---
status: completed
depends_on: [002]
parallel: false
conflicts_with: [001, 002, 004, 005, 006]
---
# Storage slide imagery

Scope: `src/decks/week-02.deck.mdx` (lines ~67–98, the three storage
slides), `src/decks/assets/week-02/*.jpg` (new files),
`src/decks/assets/week-02/SOURCES.md`.

Read epic.md §3 in full before starting.

1. Add a photograph of refrigerator crisper-drawer / refrigerated produce
   storage beside `## Refrigerated`.
2. Add a photograph of cool, dark, unrefrigerated storage (potatoes,
   onions, or winter squash out of the fridge) beside `## Not
   refrigerated`.
3. Optionally, one image for `## Humidity setting` if a suitable free
   image exists illustrating the vent-open/closed distinction — skip
   rather than force it.
4. Leave the existing sourced claims (FDA 4 °C / −18 °C, University of
   Arkansas humidity guidance) exactly as they are — images illustrate
   them, they don't replace or restate them.
5. Every new image: same sourcing process as task 002 — Commons or another
   free source, visually checked, EXIF-checked, ~1280px wide, alt text,
   one `SOURCES.md` line each.

Done when: both required storage slides carry a new photograph, existing
sourced text is unchanged, and every new image is credited in
`SOURCES.md`.
