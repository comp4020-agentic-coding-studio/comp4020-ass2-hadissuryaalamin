# 003 — Storage imagery: update

## Images added

- `src/decks/assets/week-02/onion-storage.jpg` — added beside `## Not
  refrigerated`. Shows bunches of dry onions, roots and stalks still
  attached, hung from roof beams in a dark, unheated storage room.
  Source: Wikimedia Commons, File:Onion Storage.jpg, CC BY-SA 4.0,
  author S.Elaiyaraja. Resized to 960px wide.
- `src/decks/assets/week-02/crisper-drawer.jpg` — added beside
  `## Refrigerated`. Shows a refrigerator crisper drawer packed with
  apples, oranges, lemons and limes; original USDA/FSIS caption ties it
  directly to keeping the fridge at 40 °F/4 °C or below, matching the
  slide's FDA claim. Source: Wikimedia Commons, File:Food Safety - Cut
  Waste in Refrigerators (20200608-FSIS-LSC-0138).jpg, public domain
  (USDA/FSIS, photo by Lance Cheung). Resized to 960px wide.

Both are genuine in-flow `![alt](...)` markdown images (not `![bg ...]`
backgrounds), each with descriptive alt text, credited in
`src/decks/assets/week-02/SOURCES.md` in the file's existing format.
Existing sourced text (FDA 4 °C/−18 °C temperatures, University of
Arkansas humidity guidance) was left unchanged — the images illustrate
the claims, they don't replace or restate them.

## Humidity setting — skipped

No image was added to `## Humidity setting`. Targeted Wikimedia Commons
searches for a photo that clearly shows the vent-open/vent-closed
distinction on a crisper drawer did not turn up a genuinely suitable
free image — candidates were either ambiguous (no visible vent
mechanism), off-subject, or of dubious authenticity. Per the task's
explicit guidance (and the precedent set in task 002, which skipped an
image for `batonnet` rather than force a bad fit), this slide was left
text-only.

## Verification

- `pnpm check` (typecheck, production build with axe + link checking,
  deck structural check, spec tests): **all green**.
  - Build: 48 pages, no accessibility violations, no broken links, 12
    decks checked with no structural violations.
  - Vitest: 2 test files, 11 tests, all passed.
- Screenshots taken against the dev server
  (`/comp4020-ass2-hadissuryaalamin/decks/week-02/`) at desktop
  (1280×800) and mobile (375×812) viewports, navigating reveal.js
  slide-by-slide to `Not refrigerated`, `Refrigerated` and `Humidity
  setting`. Confirmed at both viewports:
  - `onion-storage.jpg` renders under the `Not refrigerated` bullets,
    no overflow, no text covered.
  - `crisper-drawer.jpg` renders under the `Refrigerated` bullets, no
    overflow, no text covered.
  - `Humidity setting` renders as text-only, as intended.
  - No page errors or console errors during navigation.

## Scope discipline

- Did not touch `src/decks/week-07.deck.mdx`.
- Did not redo or revert tasks 001/002 (background-path fix, cut-shapes
  slides).
- No changes outside this task's file scope
  (`src/decks/week-02.deck.mdx`, `src/decks/assets/week-02/*.jpg`,
  `src/decks/assets/week-02/SOURCES.md`).
