# Task 003 — week 12 plated-presentation image: progress log

Environment note: see `updates/001.md` for the full account of why this job
could not write into the real worktree or run any shell command against it.
The content below describes what was authored, not what was verified
live — no `astro dev`/Playwright walk was possible in this job.

## What changed

`src/decks/week-12.deck.mdx`, "## How the examination reads the plate"
slide:

- Replaced the `![bg right:32% contain](.../plated-presentation.jpg)`
  background directive with an in-flow `<img>` placed after the existing
  paragraph (the slide's argument: the plate as evidence for preparation
  decisions) — body text before image, matching the deck's established
  pattern.
- New markup:

  ```html
  <img src="../../src/decks/assets/week-12/plated-presentation.jpg" alt="A sliced breaded pork cutlet on a white plate beside a mound of shredded cabbage and a dab of mustard, on a wooden counter with condiment bottles and a glass of beer blurred in the background." style="max-height: 220px; width: auto; margin: 10px auto; display: block;" />
  ```

## Alt text — viewed the actual file, did not invent

Read `src/decks/assets/week-12/plated-presentation.jpg` directly. It shows
a sliced, breaded and fried cutlet (visually a tonkatsu-style pork cutlet)
on a white plate, next to a mound of finely shredded raw cabbage and a
small dab of yellow mustard; the setting is a wooden counter with
condiment bottles, a folded white towel, a glass of beer and a mug blurred
in the background. Alt text was written to describe what is actually on
the plate (the evidence the slide's prose refers to — cut, portioning,
plating) without naming a specific dish/cuisine the slide's own prose does
not claim, since the slide's argument is about preparation evidence in
general, not this particular dish.

## Optional deepening — none proposed for week 12

The epic's optional §2 candidate is scoped to week 11 only (a two-batch
comparison photo for that week's specific exercise). No equivalent was
invented for week 12; this task made no additional imagery decision beyond
the required conversion.

No new image file, no `SOURCES.md` change —
`plated-presentation.jpg`'s existing credit line stands untouched.

## Not run

`astro dev` / Playwright viewport walk to confirm the `max-height: 220px`
budget clears the fixed 1280×720 canvas at both 1280×800 and 375×812. Same
starting-point value as task 002, for the same reason (matches weeks34
task 010's precedent), not independently measured. **Must be verified by
the coordinator.**

## Files touched (in scratch, pending move — see updates/001.md for paths)

- `src/decks/week-12.deck.mdx` (shared file with task 001's path fix)

`src/decks/assets/week-12/SOURCES.md` and every image file under
`src/decks/assets/week-12/` are untouched.
