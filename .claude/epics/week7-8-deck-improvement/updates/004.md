# Task 004 — verification and integration (Phase 2)

Ran the full scope after both lanes (002, 003) were applied.

- `pnpm check`: typecheck, production build (48 pages, no axe violations,
  no broken links), astromotion deck-check (12 decks, no structural
  violations), vitest (11/11 passed). Green.
- Slide-by-slide Playwright walk of both decks at 1280×800 and 375×812,
  with `pageerror`/`console.error` listeners attached: zero errors at every
  slide, both decks, both viewports.
- Found and fixed a real clipping defect: week 7's blade-anatomy SVG
  overflowed the 800px viewport at 1280×800 (`bottom: 884.2px`) because its
  explicit `width="640"` attribute set the rendered size before the
  `max-height` CSS could engage. Not caught by `pnpm check` — astromotion's
  deck-check only measures overflow on text-bearing elements, never
  images/SVGs (same defect class as week9-10's `meat-thermometer.jpg`
  finding). Fixed by reducing `width` and `max-height` together; re-verified
  clear (`bottom: 782.2px`). Week 8's sequencing SVG had no such issue.
- `SOURCES.md` cross-check: `assets/week-07/SOURCES.md` (3 lines) and
  `assets/week-08/SOURCES.md` (2 lines) each have exactly one credit line
  per asset file present on disk, and vice versa. Neither new SVG needed an
  entry (original artwork, no external asset file).
- Grepped `src/content/sessions/08-timed-mise-en-place.mdx`: both USDA FSIS
  links still present, unchanged.
- `git diff --stat` against the epic's start point: only
  `src/decks/week-07.deck.mdx`, `src/decks/week-08.deck.mdx`, and files
  under `.claude/epics/week7-8-deck-improvement/` changed. `week-02.deck.mdx`,
  `src/content/lectures/week-07.md`, `src/content/lectures/week-08.md`,
  `src/content/sessions/07-the-knife.md`,
  `src/content/sessions/08-timed-mise-en-place.mdx`,
  `src/content/people/dominic-achterberg.md`, the storage calculator, and
  `spec/*.test.ts` are all untouched, confirmed.
- `pnpm check:evidence`: fails only on the pre-existing, unrelated
  PROCESS.md template/citation gate (same failure mode already present
  before this epic — not caused by this epic's scope).
- Definition of Done (`epic.md`): all points satisfied, including the
  `![bg …]` alt-text requirement, which is architecturally unsatisfiable as
  literally written under this astromotion version but already met in
  substance (all bg images in both decks are decorative-only, per the
  epic's own accessibility rule) — flagged, not silently resolved.

Status: **completed**.
