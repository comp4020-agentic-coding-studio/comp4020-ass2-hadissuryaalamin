# Task 003 — week 8 content (lane B)

## Alt text for week 8's two `![bg …]` directives — same finding as week 7

Identical architectural finding as task 002: `![bg …]` directives cannot
carry alt text in this astromotion version. Both of week 8's bg images
(restaurant-kitchen.jpg on the title slide, plated-dish.jpg on the "Made to
order" slide) are decorative/atmospheric only — no slide content depends on
either, satisfying the epic's own accessibility rule for decorative
backgrounds even though the literal "add alt text" instruction cannot be
carried out under this astromotion version. Not applied — an `alt:` token
would be silently discarded and would misrepresent the change.

## Sequencing timeline diagram — applied and verified

Original inline SVG in the "Sequencing" slide: four-box left-to-right
timeline strip — "Finished ahead" → "Held" → "Made to order" → "Sequenced"
— connected by arrow markers, restating the four categories this and the
two preceding slides establish, in the order they're introduced.
`role="img" aria-label="Timeline strip showing the four preparation
categories in order: finished ahead, held, made to order, sequenced"`.

Verified via Playwright walk at 1280×800 and 375×812: renders cleanly with
no clipping at either viewport (`bottom: 643.3px` at 1280×800, well clear
of the 800px canvas; `477.3px` at 375×812). No fix-up needed — the
`width="700"`/`max-width:100%` combination already used for week 7's
diagram scaled correctly against the deck's actual rendered canvas, and the
local `marker id="seq-arrow"` did not collide with anything else on the
page (MDX inlines each slide independently).

No new file added under `assets/week-08/`; no new `SOURCES.md` entry
needed.

## Status

Applied, verified (Playwright, both viewports, zero page errors), no
clipping found. **completed**.
