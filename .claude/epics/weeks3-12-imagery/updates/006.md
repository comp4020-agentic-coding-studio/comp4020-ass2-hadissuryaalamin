# Lane E — weeks 11-12: update

## Summary

Both decks now carry 6 images/diagrams each and use `.columns` in multiple
places. No prose, figure, table or citation was changed. No baseline slide
outside my scope was edited.

## Week 11 (`src/decks/week-11.deck.mdx`)

- **Two batches, one stated variable** — added a hand-authored inline SVG
  flow diagram (Batch A / Batch B → shared "same recipe, heat, time, cook" →
  Result A / Result B → "Compared on: colour, aroma, flavour, texture"
  caption). Full-width, block `<div>` wrapper (not flex — flex breaks SVG
  scaling on `section` elements per lane C/precedent). This is the two-batch
  comparison treatment the task file asked for. No staged/faked photograph
  was used: no honest matched "before/after" photo pair exists for a
  variable that differs per group, so a diagram was the correct call per the
  task's explicit instruction.
- **What the variable can be** — wrapped existing text in `.columns`, added
  `flank-steak-grain.jpg` (raw flank steak on a board, annotated "direction
  of grain" / "against grain") — directly illustrates bullet 2 ("a cut made
  across the grain instead of with the anatomy"). Before: no image. After:
  305px canvas height at 1280×720.
- **Why colour and time are not reliable** — wrapped existing text in
  `.columns`, added `ground-meat.jpg` (raw ground beef from a grinder) —
  illustrates the paragraph's point about grinding distributing bacteria
  through the whole piece. Before: no image. After: 363px.
- **Recording the result** — added a hand-authored inline SVG "record card"
  diagram (four labelled fields: variable tested, held identical, difference
  observed, conclusion), full width, below the existing paragraph.
- **The thermometer decides** — left unchanged. Already has an image at
  220px (≥200px floor) and a 3-row table; converting to `.columns` risked
  squeezing the table into 544px width for no required gain, so it was left
  alone (in scope, but a deliberate no-op).
- Untouched: title slide (`frying-pan-cooking.jpg`), "Everything else held
  identical" (no image, not required), reading/quote/closing slides.

Image/diagram count: bg photo + thermometer photo (untouched) + flank-steak
photo (new) + ground-meat photo (new) + two-batch SVG diagram (new) +
record-card SVG diagram (new) = **6**.

## Week 12 (`src/decks/week-12.deck.mdx`)

- **Preparation, not the plate in isolation** — wrapped in `.columns`, added
  `kitchen-prep-station.jpg` (mise en place, stainless bowls of staged
  sauces/spices in a commercial kitchen — one blurred leg, no face). Before:
  no image. After: 408px.
- **How the examination reads the plate** — upgraded the existing
  `plated-presentation.jpg` from a bare inline `<img>` (`max-height: 220px`)
  to a `.columns` layout. Before: 220px. After: 408px.
- **The standards do not change** — wrapped in `.columns`, added
  `fridge-thermometer.jpg` (dial fridge/freezer thermometer with marked
  "SAFE" zone, no people). Before: no image. After: 397px.
- **Why a clock and an audience are a real test** — wrapped in `.columns`,
  added `kitchen-timer.jpg` (close-up of a mechanical oven timer dial, no
  food or people visible). Before: no image. After: 408px.
- **What week 6 predicted** — added a hand-authored inline SVG
  "correspondence" diagram: the three parts of the week 6 written
  prediction (preparation process / nutritional composition / sensory
  description) each pointing to their corresponding check against the week
  12 result. This visualises the heading's own claim ("correspondence with
  the written account") without asserting any marking criterion the
  assessment page does not state.
- Untouched: title slide (`cooking-competition.jpg` — re-checked visually;
  shows only hands/arms plating food, no identifiable face despite the
  Commons filename referencing a real chef, so it is safe as a decorative
  background), "Right for the wrong reason...", "What counts as
  demonstrating the course", quote/closing slides.

Image/diagram count: bg photo (untouched) + plated-presentation (upgraded) +
kitchen-prep-station (new) + fridge-thermometer (new) + kitchen-timer (new) +
correspondence SVG diagram (new) = **6**.

## Skips / rejected candidates

- A "gas flame adjustment" photo was considered for week 11's opening
  ("The first week that uses heat") but no honest Commons candidate was
  found that wasn't either staged-looking or a duplicate crop of an
  already-used image. Slide left without a new image; this is acceptable
  since the 6-image target is already met by other slides in the deck.
- No staged "two batches side by side" photograph was used anywhere — see
  the diagram note above.

## Sourcing

5 new files, all Wikimedia Commons, all resized to ~960px wide, all visually
inspected, all EXIF-checked (no AI-generation tells; camera models present
where EXIF exists — Panasonic DMC-FZ1000, Canon PowerShot SD800 IS, Samsung
Galaxy A16 — all real, plausibly-dated models), all credited one line per
file in the matching week's `SOURCES.md` in this same commit:

- `src/decks/assets/week-11/flank-steak-grain.jpg` — File:Flank steaks
  showing grain.jpg — CC BY-SA 4.0 — Kittycataclysm
- `src/decks/assets/week-11/ground-meat.jpg` — File:Minced meat.jpg — CC0 —
  Erwin (via Pixabay)
- `src/decks/assets/week-12/kitchen-prep-station.jpg` — File:Mise en place
  for hot station.jpg — CC BY-SA 2.0 — Charles Haynes
- `src/decks/assets/week-12/kitchen-timer.jpg` — File:Kitchen timer in a
  oven.jpg — CC0 — Agilix
- `src/decks/assets/week-12/fridge-thermometer.jpg` — File:Rubbermaid R80DC
  Refrigerator Freezer Thermometer (40251799920).jpg — CC BY 2.0 — Tony
  Webster from Minneapolis, Minnesota, United States

Both `SOURCES.md` files now match one-to-one with the files on disk in their
directories.

## Verification

Ran `astro dev --port 4335 --ignore-lock` (with `ASTRO_DEV_BACKGROUND=1` to
bypass the CLI's AI-agent auto-background detection, which otherwise refuses
`--ignore-lock`) alongside lane C/D's own dev servers, untouched. Used a
temporary Playwright script (removed before finishing, along with the
`playwright` devDependency — `git diff package.json pnpm-lock.yaml` shows no
content diff) to visit every slide of both decks at 1280×720 and 375×812,
navigate by one-based hash, wait on `document.fonts.ready`, and measure
canvas-relative dimensions (`scale = section.getBoundingClientRect().height /
720`). Checked: page errors/console errors (none), `img.naturalWidth > 0`
(all true), every image's canvas-relative height (all new/upgraded images
297px-408px, all ≥ 200px), and overflow (`section.scrollHeight >
section.clientHeight + 4px`).

**Baseline overflow found (pre-existing, not caused by this lane, not
fixed):** week 11's "The thermometer decides" slide overflows by 7px
(scrollHeight 727 vs clientHeight 720) at both viewports. This slide was not
edited in this lane. It is a small, likely invisible-on-canvas overflow
(clipped silently per the fixed-canvas design), flagged here for task 007's
awareness per the epic's baseline-overflow-check instruction.

All other 64 of 66 slide×viewport combinations checked (2 decks × ~16-17
slides × 2 viewports) showed zero overflow and zero errors after my fixes
(two of my own diagrams initially overflowed and were shrunk/redesigned
before landing — see commit history).
