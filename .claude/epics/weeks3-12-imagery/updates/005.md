# Task 005 — Lane D (weeks 9 and 10): fruit and the cold chain

## Priority-one fix

`meat-thermometer.jpg` on week 10's *Poultry, fish and red meat* slide rendered
at **89×80 canvas px** — the second-smallest image in the course. The intro
line and holding-time table were left completely unchanged and full-width; the
two trailing paragraphs were moved into a `.columns` left cell, with the same
image placed in the right cell at the standard `.columns` size. Measured after
the change: **544×480 canvas px**, roughly a 6.75x increase in height. No
prose was altered.

## Week 10 — other changes

- *The two-hour rule* (image-free before): added `clock.jpg` (a digital
  kitchen timer, blank LCD) via `.columns`. Both original paragraphs kept
  unchanged in the left cell.
- *Worked example* (image-free before): added `wall-clock.jpg` (a close-up
  analogue clock face) via `.columns`. Both original paragraphs kept unchanged.
- *The range, and how fast it works* (image-free before): no honest
  photograph was found that didn't duplicate an existing thermometer/timer
  image already used elsewhere in the course (week 8's digital kitchen timer,
  weeks 3/4's fridge-thermometer.jpg, week 10's own meat-thermometer.jpg).
  Added an inline SVG diagram instead (tier-2 of the fallback ladder: drawn
  diagram over no visual at all) — a 0-100°C temperature scale with the
  4-60°C danger zone highlighted, matching week 8's existing SVG timeline-strip
  style. Text unchanged.
- *What this week covers, and what it doesn't*: `handwashing.jpg` moved from
  an inline `max-height: 200px` treatment into the standard `.columns` right
  cell (`max-height: 480px; width: 100%; object-fit: contain;`). Text
  unchanged.
- *One argument, not four pieces of advice*: **skipped, deliberately.** This
  is an abstract synthesis slide summarising weeks 2, 3, 4 and 9 as "one
  argument" — it has no single natural photographic or diagrammatic referent
  that wouldn't be arbitrary filler. Left image-free per the epic's own "skip
  rather than force a decorative photo" instruction.

Week 10 now carries six images: `refrigerator-interior.jpg` (unchanged, title
bg), `meat-thermometer.jpg` (existing, resized in-layout), `handwashing.jpg`
(existing, re-laid-out), `clock.jpg` (new), `wall-clock.jpg` (new), plus the
new SVG diagram as a non-photographic visual on an otherwise image-free slide.

New sources added to `src/decks/assets/week-10/SOURCES.md`:

- `clock.jpg` — File:Small kitchen timer.jpg —
  <https://commons.wikimedia.org/w/index.php?curid=28523321> — CC BY 3.0 —
  Douglas Paul Perkins (61KB)
- `wall-clock.jpg` — File:Analog clock 2014.jpg —
  <https://commons.wikimedia.org/w/index.php?curid=54916988> — CC BY 2.0 —
  Markus Spiske (50KB)

## Week 9 — changes

- *Two ripening behaviours, not species*: removed the `![bg right:32%
  contain](.../bananas.jpg)` panel. This slide, at baseline (before any of my
  edits), overflowed the 720px canvas at **761.4px** — a pre-existing defect,
  not something I introduced. Since prose cannot be shortened under the hard
  rule and the panel was the only other lever, the panel was removed and its
  image reused on a new slide instead (see below). Text on this slide is
  otherwise unchanged.
- New slide **"Climacteric next to non-climacteric"**, inserted after *The
  other half*: a `.compare` pair — `bananas.jpg` (reused from the removed bg
  panel) against new `grapes.jpg` — directly illustrating the week's central
  climacteric-vs-non-climacteric argument. One new sentence of caption prose
  was added under the pair (not a change to any pre-existing prose, figure,
  table or citation).
- *Counter first, then refrigerator*: converted to `.columns`, original prose
  kept unchanged in the left cell, new `avocado.jpg` added to the right cell.
- *Refrigerator from the start*: converted to `.columns`, original prose kept
  unchanged in the left cell, new `cherries.jpg` added to the right cell.
- *Reading the spread* / new slide **"One of the five"**: the pre-existing
  plain-markdown `![alt](./assets/week-09/oranges.jpg)` line was relocated
  from the bottom of *Reading the spread* onto its own new slide immediately
  after. The two original paragraphs on *Reading the spread* were left
  completely untouched. The markdown reference string and its alt text were
  **not** changed — per the hard rule, the `./assets/week-09/...` form is
  correct as written and must never be "fixed" to the `../../src/decks/...`
  form used by `![bg]`/`<img>` forms.

  This relocation surfaced a second baseline-adjacent overflow: on its
  original slide the image, combined with the two paragraphs above it,
  overflowed at **1057.4px**; on the new dedicated slide, the image alone
  still overflowed at **799.8px**, because `deck.css` has no default `img`
  sizing rule for the plain-markdown form and the source file's native size
  (960x640) is too tall under a heading-only slide. Rather than wrap the
  markdown image in a styled container (no such wrapped-plain-markdown
  pattern exists anywhere else in the deck files — see week-02.deck.mdx's
  unstyled `onions.jpg` for the established convention), the underlying
  `oranges.jpg` asset file itself was resized from 960x640 down to **780x520**
  (quality 88, 66KB). The markdown line referencing it is byte-for-byte
  unchanged. Re-verified: no overflow at either viewport.

Week 9 now carries six images: `apple.jpg` (unchanged, title bg), `oranges.jpg`
(existing, resized in place, relocated to its own slide), `bananas.jpg`
(existing, moved from bg-panel into the new compare slide), plus new
`grapes.jpg`, `avocado.jpg`, `cherries.jpg`.

New sources added to `src/decks/assets/week-09/SOURCES.md`:

- `grapes.jpg` — File:2021-07-11 13 08 13 Green table grapes in the Franklin
  Farm section of Oak Hill, Fairfax County, Virginia.jpg —
  <https://commons.wikimedia.org/w/index.php?curid=107428806> — CC BY-SA 4.0
  — Famartin (141KB)
- `avocado.jpg` — File:Nice Avocados.jpg —
  <https://commons.wikimedia.org/w/index.php?curid=29162382> — CC0 —
  ProjectManhattan (182KB)
- `cherries.jpg` — File:Bunch of cherries on a white plate (7599157446).jpg —
  <https://commons.wikimedia.org/w/index.php?curid=143112227> — CC0 —
  Picdrome Public Domain Pictures (85KB)

No week-9 prose, figure, table or citation was changed. The FDC-ID nutrition
table and the `FoodData Central` link label are untouched.

## Baseline overflow found (pre-existing, independent of my edits)

Two baseline overflow bugs were found while checking weeks 9-10 in a browser,
consistent with the overflow-defect class lane C previously found in week 7:

1. Week 9, *Two ripening behaviours, not species* — `bg right:32% contain`
   panel pushed content to 761.4px against a 720px canvas. Fixed by removing
   the panel (see above; text unchanged).
2. Week 9, *Reading the spread* (original location of the oranges.jpg plain-
   markdown image) — overflowed at 1057.4px. Fixed by relocating the image to
   its own slide and, after that alone still overflowed at 799.8px, resizing
   the underlying asset (see above; markdown/text unchanged).

Neither bug involved a slide task 007 or another lane owns, and both are now
verified clean.

## Verification

- Numeric check: canvas-relative script (`.reveal .slides > section.present`
  bounding box, `naturalWidth > 0`, `pageerror`/`console` listeners) run
  against `astro dev --port 4334` across both decks, both viewports (1280x720,
  375x812) — **84 slide-viewport combinations, all PASS**: no overflow, no
  image under 200px canvas height (except deliberately full-bleed bg images),
  no broken images, no page or console errors.
- Visual check: screenshots reviewed by eye for every touched slide at
  1280x720 (all clean — no overlap, images well-sized, text fits with margin)
  and spot-checked at 375x812 (letterboxed identically to desktop in canvas
  coordinates, as expected — Reveal scales one fixed canvas).
- Neither `pnpm build` nor `pnpm check` was run (task 007's responsibility).

## Note for task 007: Playwright availability

By the time I started, the repo's own `node_modules/playwright` had already
been removed (lane E's cleanup after finishing weeks 11-12, confirmed via
`git log`). Rather than touch the repo's own `package.json` /
`pnpm-lock.yaml` (forbidden), I created an isolated scratch npm project
**outside the repo** at
`C:\Users\hadis\AppData\Local\Temp\pw-scratch-lane-d` (own `package.json`,
`pnpm add playwright@1.63.0`), and had my verification script import it via
`createRequire(import.meta.url)` with an absolute path into that scratch
`node_modules`, instead of a bare `import { chromium } from "playwright"`.
Chromium itself did not need re-downloading — it was already cached system-
wide at `C:\Users\hadis\AppData\Local\ms-playwright`. If task 007 finds
Playwright similarly absent from the repo's own `node_modules` at final
verification time, the same technique (external scratch project +
`createRequire` absolute import) avoids touching `package.json` /
`pnpm-lock.yaml` while still running full Playwright checks. The scratch
project is outside the repo and does not affect `git status`; I am leaving it
in place in case task 007 wants to reuse it, but it can be deleted freely.

## Cleanup

`tmp-verify-d/` (verification script, results.json, screenshots) is untracked
and has been deleted before finishing, matching the epic's established
convention (lane E's equivalent `tmp-verify/` directory).

## Files touched (staged, this lane's paths only)

- `src/decks/week-09.deck.mdx`, `src/decks/week-10.deck.mdx`
- `src/decks/assets/week-09/SOURCES.md`, `src/decks/assets/week-10/SOURCES.md`
- `src/decks/assets/week-09/oranges.jpg` (resized in place)
- `src/decks/assets/week-09/grapes.jpg`, `avocado.jpg`, `cherries.jpg` (new)
- `src/decks/assets/week-10/clock.jpg`, `wall-clock.jpg` (new)
- `.claude/epics/weeks3-12-imagery/005-lane-d-weeks-9-10.md` (status flip)
- `.claude/epics/weeks3-12-imagery/updates/005.md` (this file)

`package.json`, `pnpm-lock.yaml`, `src/decks/theme.css`, and every lane-E path
were not touched.
