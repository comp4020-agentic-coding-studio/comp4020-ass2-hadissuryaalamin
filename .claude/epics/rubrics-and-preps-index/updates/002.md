# Task 002 — Preps index page: update log

## Old text (removed)

> The internal collection and URL stay `sessions`. Set the visible singular and
> plural names once in `src/site-config.ts` and use the language your course
> deserves everywhere readers see it.

This was developer-facing boilerplate (instructions for whoever builds the
site) rendering live to students. It named no course content at all.

## New text (in place)

> Each week below pairs a lecture with a session. The lecture argues for a
> preparation decision; the session is where it is exercised, not restated.
> Seven sessions — weeks 2, 3, 4, 5, 8, 9 and 10 — run a calculator against
> figures for an ingredient the student chooses: a macronutrient split in
> weeks 2, 3, 4 and 9, a safe-until time in weeks 5, 8 and 10. The remaining
> five sit at the bench instead of on screen. Week 7 checks a knife's grip
> and edge against reference figures for hardness and angle. Week 11
> applies heat for the first time, to test whether preparation rather than
> technique decides the result. Week 12 rehearses that same test under the
> conditions of the graded practical that follows it. Weeks 1 and 6 carry
> no exercise at all, and each says why. A session with nothing yet to
> exercise runs no calculator; a lecture with no session behind it would go
> untested.

`<SessionsGrid />` and the `sessionLabels`-driven `title`/`description` on
`ContentLayout` are untouched. `src/site-config.ts` was not touched.

## Verification — session files read to confirm the Prep/lecture distinction

- `src/content/sessions/01-getting-started.md` (week 1, `tutorial: false`) —
  has a "Why there is no tutorial" section: week 1 only argues the thesis, so
  there is no decision yet for an exercise to check.
- `src/content/sessions/02-vegetables.mdx`, `03-poultry-and-fish.mdx`,
  `04-red-meat.mdx`, `09-fruit.mdx` — confirmed via `grep -l MacroCalculator`
  to import `MacroCalculator` (macronutrient split exercises).
- `src/content/sessions/05-aromatics-and-oil.mdx` (week 5, `tutorial: true`) —
  read in full. Imports `StorageCalculator`; opening line: "Deciding how long
  a chopped or crushed allium can wait ... is a preparation decision distinct
  from what the cutting itself does to its flavour, which is the lecture's
  subject." Confirms both the calculator claim and the lecture/session split.
- `src/content/sessions/08-timed-mise-en-place.mdx`,
  `10-food-safety-and-the-cold-chain.mdx` — confirmed via
  `grep -l StorageCalculator` to import `StorageCalculator` (safe-until-time
  exercises), alongside week 5.
- `src/content/sessions/06-kitchen-tools-and-surfaces.md` (week 6,
  `tutorial: false`) — read in full. Has a "Why there is no tutorial"
  section: the week's decisions (board material, maintenance) are judged by
  comparison and inspection, not a calculator.
- `src/content/sessions/07-the-knife.md` (week 7, `tutorial: false`) — read
  in full. Opens "The lecture describes a knife. This session is where one
  is used." Body is a bench "Weigh-in" against reference edge-angle and
  Rockwell-hardness figures, matching "checks a knife's grip and edge against
  reference figures for hardness and angle" in the new text.
- `src/content/sessions/11-first-heat.md` (week 11, `tutorial: false`) — read
  in full. "This week is the first to use it [heat], and it uses it as an
  instrument ... the question being tested is whether preparation, not
  technique, decides the result." Matches the new text exactly.
- `src/content/sessions/12-the-live-test.md` (week 12, `tutorial: false`) —
  read in full. It is explicitly a rehearsal for the graded practical, not
  itself a cooking session — "What leaves this session is not a grade ... but
  a checked plan." The new text says week 12 "rehearses that same test," not
  that it applies heat, to avoid overclaiming.
- Cross-checked the full `tutorial: true` set with
  `grep -rl "tutorial: true" src/content/sessions/` → weeks 2, 3, 4, 5, 8, 9,
  10, matching the epic's and task's stated seven.

## Note for the verification task (003)

`src/content/sessions/12-the-live-test.md` (out of my scope — a session
file) contains this sentence, unrelated to my edit but relevant to
workstream A: "The assessment's weighting reflects this: execution under the
time limit is the single largest component, because it is the one condition
this practical adds that no earlier week could test." Once task 001 lands
(Execution under time constraint drops from 40 to 20, no longer the largest
criterion at 30/20/30/20), this sentence in the *session* file becomes false
in the same way the epic already flagged for the *assessment* file's "How it
is marked" paragraph — except this occurrence is in a file the epic's
constraints put off-limits to both build tasks ("do not touch ... any
session or lecture file"). Task 003 should decide whether this is worth
flagging back to the user as a follow-up, since neither 001 nor 002 is
permitted to fix it under current scope.
