# Task 001 — Tester feedback criterion on the public kitchen

File changed: `src/content/assessments/the-public-kitchen.md`.

## Criteria and weights (now)

| Criterion | Weight |
|---|---|
| Preparation decisions | 30 |
| Execution under time constraint | 20 |
| Correspondence with the earlier account | 30 |
| Tester feedback | 20 |

Sums to exactly 100. `related: [invent-a-recipe]`, `weight: 60`, `week: 12`
and `due:` left unchanged.

## `description:` frontmatter (rewritten to name all four)

> The week 12 practical, assessed live — preparation decisions, execution
> under time constraint, whether the result matches what was predicted, and
> how the student handles an independent taster's report of it.

## New `spec:` line

> the student presents a sensory report of the finished dish from a taster
> other than themselves, on the same four terms as the week 6 prediction,
> and states where the two accounts agree or diverge

## "What is examined" — Tester feedback paragraph

> **Tester feedback.** The finished dish is also tasted by someone other than
> the student — the examiner, or another attendee at the same session — who
> gives a sensory report of it on the same four terms as the week 6
> prediction: colour, aroma, flavour, texture. This is not a measure of
> whether the dish was liked; the course's position, argued in week 5, is
> that nothing about preparation depends on taste perception, and a tester's
> approval carries no marks. What is assessed is narrower and more specific
> than correspondence: whether the student can obtain a structured report
> from a palate that is not their own and reconcile it with the week 6
> prediction, rather than relying only on their own tasting of the result.

Distinction from Correspondence: Correspondence is the student's own
comparison of the finished dish against their week 6 prediction (self
report). Tester feedback is obtaining and reconciling a report from a
palate that is not the student's own. The paragraph says this explicitly
("narrower and more specific than correspondence... rather than relying
only on their own tasting") so a reader cannot mistake the two. No real
person is named; the tester is described only as a role ("the examiner, or
another attendee at the same session"), consistent with the project rule
and the epic's decision 7.

## Rewritten "How it is marked" paragraph (no longer claims Execution is
heaviest)

> The `marking` block above renders as a criterion table. Preparation
> decisions and correspondence with the earlier account carry the most
> weight, at 30 each: the course treats "deciding well" and "knowing whether
> the decision worked" as two halves of the same skill. Execution under time
> constraint and tester feedback are weighted evenly at 20 each — execution
> because the time limit is a condition of the session rather than a
> discipline separate from preparation, and tester feedback because it
> corroborates the student's own correspondence claim against an outside
> report rather than standing as an independent judgement of the dish.

Checked by reading: no sentence anywhere on the page still claims Execution
carries the most weight.

## Not done (out of scope for this task, per epic constraints)

- Did not run `pnpm build` or `pnpm check` — task 003 owns the only
  production build.
- Did not touch `invent-a-recipe.md`, `src/content.config.ts`,
  `src/components/MarkingModel.astro`, `src/site-config.ts`, or any session,
  lecture or deck file.

## Note for task 003 (verification)

- Confirm the rendered rubric table on `/assessments/the-public-kitchen/`
  shows four rows (30/20/30/20) via `MarkingModel.astro`.
- Confirm `dist/api/the-public-kitchen.json` (or equivalent) reflects the
  new criteria sum and the fifth spec line.
- `git diff --stat` should show only this file plus this task's own
  `.claude/epics/...` files touched by workstream A.
