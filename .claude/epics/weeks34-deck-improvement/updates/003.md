# Task 003 — week 3 storage/handling imagery: progress log

## What was added

`src/decks/week-03.deck.mdx`:

- `## Poultry` — added a sentence connecting the 4.4 °C table value to the
  need to verify it (not assume it), then `fridge-thermometer.jpg`: a hand
  holding an appliance thermometer clipped to a shelf, dial needle sitting
  just below the 40 °F REF mark, raw poultry visible below. The photo
  carries the check the slide already states, not just the material.
- `## Fish and shellfish` — added `fish-counter-ice.jpg` after the existing
  bullets: whole fish of several species bedded in crushed ice at a market
  counter. Directly illustrates the slide's own rule ("onto ice or into the
  fridge within 2 hours of purchase").

`## Cold chain` and `## Cross-contamination` were left unillustrated —
searched Commons for a cross-contamination candidate (colour-coded cutting
boards used correctly, not just displayed) and found nothing suitable that
wasn't either staged/AI-styled stock or ambiguous about what it was showing.
Per the epic's "skip rather than force" guidance, these two slides are
unchanged. Two new photographs meets the task's minimum for week 3.

Both new images use per-image inline `max-height` (no new shared CSS class):
140px for the thermometer, 190px for the fish/ice. Both were first tried at
260px and clipped past the fixed 720px canvas bottom (confirmed via
Playwright `getBoundingClientRect()`); reduced and re-measured until each
image's bottom edge sits comfortably above the 760px absolute limit.

## Sources cited

- **Wikimedia Commons — File:Food Safety - Cut Waste in Refrigerators
  (20120106-OC-AMW-0073).jpg**
  (<https://commons.wikimedia.org/wiki/File:Food_Safety_-_Cut_Waste_in_Refrigerators_(20120106-OC-AMW-0073).jpg>)
  — public domain (U.S. Government work), author USDA. Original 5436×4080,
  no EXIF anomalies consistent with AI generation, resized to 960×721
  (89 KB), saved as `src/decks/assets/week-03/fridge-thermometer.jpg`. A
  different subject/framing from week 10's `meat-thermometer.jpg` (a
  refrigerator dial thermometer on a shelf, not a probe thermometer in
  meat).

- **Wikimedia Commons — File:Whiting, pompano, white perch, and flounder
  fish displayed for sale on ice in Lotte Plaza Market, Tampa, Florida, on
  15 January 2025.jpg**
  (<https://commons.wikimedia.org/wiki/File:Whiting,_pompano,_white_perch,_and_flounder_fish_displayed_for_sale_on_ice_in_Lotte_Plaza_Market,_Tampa,_Florida,_on_15_January_2025.jpg>)
  — CC0, author MatthewHoobin. Original 4608×2592, no EXIF anomalies,
  resized to 960×540 (127 KB), saved as
  `src/decks/assets/week-03/fish-counter-ice.jpg`.

Both credit lines appended to `src/decks/assets/week-03/SOURCES.md` in the
standard format. No existing sourced claim in the deck was altered — both
additions are new prose/images only.

## Verification

- Started a local `astro dev` instance (port 4330 — the project's shared
  dev-server lock meant an earlier instance on 4326 had to be waited out;
  it exited on its own before this one was started).
- Verified via scripted Playwright (cached `playwright` package invoked
  from an npx cache directory, matched to an installed Chromium revision,
  since Playwright is not a local project dependency): navigated to both
  changed slides (`Poultry`, `Fish and shellfish`) plus a full walk of all
  20 slides in the deck.
- Confirmed zero `pageerror`/console errors across the full walk.
- Confirmed both new `<img>` elements report `naturalWidth > 0`.
- Measured each image's `getBoundingClientRect()` against the canvas
  bounds (`top:40`/`bottom:760`): first pass at `max-height:260px`
  overflowed by 92.6px (Poultry) and 40.6px (Fish); after reducing to
  140px/190px, both now sit at `bottom:732.58`/`730.59` — inside the
  canvas with headroom.
- Screenshotted all changed slides at 1280×800; confirmed visually nothing
  is clipped and both images render as intended, with body text preceding
  each image.
- Did not run `pnpm build` or `pnpm check` (out of scope for this task).

## Files touched

- `src/decks/week-03.deck.mdx`
- `src/decks/assets/week-03/SOURCES.md`
- `src/decks/assets/week-03/fridge-thermometer.jpg` (new)
- `src/decks/assets/week-03/fish-counter-ice.jpg` (new)
- `.claude/epics/weeks34-deck-improvement/003-week3-storage-imagery.md`
  (status flip)

No `week-04*` files or `src/decks/theme.css` were touched.
