# 004 — week 3 freshness: progress

## What was added

New "Choosing well" section in `src/decks/week-03.deck.mdx`, inserted between
the "Fish portion forms" slide and the "Handling" banner (4 new content
slides + 1 banner):

1. Banner: "Choosing well"
2. "Fish, checked before it is bought" — four FDA checks (eyes, gills, flesh,
   smell), each stated descriptively with the direction of change only.
3. "Eyes and gills together, on one fish" — single labelled close-up photo
   (fallback rung 3, see below), body text before image.
4. "What the grading standard adds" — FAO EEC grading standard, covering
   convex/sunken eye shape and scale/skin condition, which the FDA source
   does not mention.

## Sources cited

- **FDA / FoodSafety.gov — "Safe Selection and Handling of Fish and
  Shellfish"** (already on the Reading slide from a prior task, reused, not
  duplicated):
  `https://www.foodsafety.gov/blog/safe-selection-and-handling-fish-and-shellfish`
  Direct `WebFetch`/`curl` return HTTP 403 (bot-blocking, not a dead page) —
  verified the page genuinely resolves via `WebSearch` (indexed content
  matches) and a proxy fetch through `https://r.jina.ai/<url>`, which
  returned full page text. Confirmed content: eyes clear/shiny, gills
  red/no odor, flesh firm/springs back, smell mild not fishy/sour/ammonia.
  Used for checks 1–4 above (eyes, gills, flesh, smell).

- **FAO — "Guide to EEC Freshness Grades"** (new citation, added to the
  Reading slide right after the FDA link):
  `https://www.fao.org/4/V7180E/V7180E14.HTM`
  Verified HTTP 200 and content via `WebFetch`. Added because the epic's
  brief assumed "convex vs sunken" eyes and "scales intact" were in the FDA
  source — they are not. Rather than fabricate those two claims or silently
  drop them, found and verified this second resolving source, which
  explicitly covers eye convexity/sunkenness, gill colour gradient, and
  skin/scale bright-vs-dull condition. The deck slide states plainly that
  this is a grading standard, not a safety threshold, and that scale
  condition specifically is not documented in the FDA source.

## Fallback ladder — rung used and why

**Rung 3 (single labelled close-up, not a fake comparison)** was used for
the eyes/gills slide, after ruling out rungs 1 and 2:

- **Rung 1 (matched fresh-vs-declining pair)**: searched Wikimedia Commons
  for a comparable pair of fresh/declining fish photographed similarly — none
  found. This was the exact sourcing risk the epic itself predicted for fish.
- **Rung 2 (labelled diagram)**: searched Commons categories ("Fish
  diagrams") and terms ("fish freshness diagram", "fish spoilage chart eye
  gill", "external anatomy fish diagram labeled") — nothing usable found.
  Skipped rather than forced, mirroring task 003's precedent of skipping an
  illustration when no suitable Commons image existed.
- **Rung 3 used**: a single labelled close-up showing both the eye and gill
  check performed together on one fish, with prose explicitly stating it is
  not a comparison. This is the exact non-pair fallback the epic names as
  viable for fish.

`.compare` class from `theme.css` was **not** used (no pair to lay side by
side) and `theme.css` was **not** touched, per scope.

## Image sourced

- **File**: `src/decks/assets/week-03/fish-gill-eye-check.jpg`
- **Commons source**: File:CSIRO ScienceImage 7341 Atlantic salmon
  gillcheck.jpg —
  `https://commons.wikimedia.org/wiki/File:CSIRO_ScienceImage_7341_Atlantic_salmon_gillcheck.jpg`
- **Licence**: CC BY 3.0, author Peter Whyte, CSIRO
- Verified via Commons API `imageinfo`: real EXIF (SONY CYBERSHOT,
  DateTime 2006:04:07 09:00:10) — no AI-generation tells.
- Viewed full-resolution download (2048×1536, 790,509 bytes) before use:
  shows a gloved hand lifting a salmon's gill cover, exposing bright red
  gill filaments and the clear eye together — matches the checks named in
  the alt text and prose.
- Resized to 960×720 (114,055 bytes, quality 85) via PIL, saved to
  `src/decks/assets/week-03/`.
- Credit line appended to `src/decks/assets/week-03/SOURCES.md` (existing 6
  lines untouched).
- Real, descriptive alt text on the `<img>` tag naming both checks shown.
- Body text (two paragraphs) placed before the image in the slide markup,
  per the "body text before image" rule — this was initially reversed and
  caused clipping (see Verification below), then corrected.

## Verification

- Local dev server: `astro dev --port 4332` (the shared project-level
  singleton on port 4327 died mid-task, owned by another lane's process;
  started a fresh one on 4332 via the Bash tool's `run_in_background`).
- Playwright (via `NODE_PATH` pointing at the npx cache, v1.63.0), viewport
  1280×800, matching the fixed 1280×720 canvas (visible bounds top:40/
  bottom:760):
  - Full-deck walkthrough (all ~24 slides, `ArrowRight` navigation):
    **zero `pageerror` and zero `console.error` events** (`ERRORS: []`).
  - New image `fish-gill-eye-check.jpg`: **`naturalWidth: 960`** (loads
    correctly, not broken).
  - New image bounding box: `bottom: 646.39`, well inside the canvas's
    760 limit — no clipping.
  - Both new paragraphs on the "Eyes and gills together" slide: bottoms at
    314.8 and 422.6, both inside canvas bounds.
  - A first pass had the image before the two paragraphs at `max-height:
    320px`, which pushed the second paragraph's bottom to 856.6 (clipped
    against the 760 limit) — fixed by shrinking the image to `max-height:
    190px`, shortening both paragraphs, and moving the image after the text.
  - Screenshot of the corrected slide viewed directly: heading, both
    paragraphs and the image all render fully within the dark canvas, no
    overlap or cutoff, image visibly shows a gloved hand at a salmon's gill
    and eye.

No `pnpm build` or `pnpm check` run (per task instructions — screenshots
and Playwright were the verification method for this task).

## Files touched

- `src/decks/week-03.deck.mdx` (new section + Reading slide FAO link)
- `src/decks/assets/week-03/SOURCES.md` (one new credit line)
- `src/decks/assets/week-03/fish-gill-eye-check.jpg` (new file)
- `.claude/epics/weeks34-deck-improvement/004-week3-freshness.md` (status
  flip)
- `.claude/epics/weeks34-deck-improvement/updates/004.md` (this file)

`week-04*` files and `src/decks/theme.css` were not touched (confirmed via
`git status --short` before staging — only week-03 files and this task's
own files appeared as changed).
