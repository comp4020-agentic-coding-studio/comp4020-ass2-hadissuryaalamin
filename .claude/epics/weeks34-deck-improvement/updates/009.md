# Task 009 — week 4: nutrition reconcile and deepen

## FDC IDs and figures, verified against USDA FoodData Central

All four items already existed, unchanged, in `src/data/macro/week-04.ts`
(no extension needed — the calculator already had exactly these four
items, matching the epic's own table). Every macro figure below was
independently re-verified against FoodData Central before being recorded
in the deck and lecture (not merely copied from the existing calculator
file), using two methods per item: the official
`api.nal.usda.gov/fdc/v1/food/<id>` endpoint (direct fetch with the
public `DEMO_KEY`, until it started rate-limiting) and, for cross-checks,
the same endpoint and the `fdc.nal.usda.gov/food-details/<id>/nutrients`
page fetched through the `r.jina.ai` rendering proxy (needed because the
FDC website is a client-rendered SPA that 404s on a bare unrendered
fetch). All methods agreed.

| Food | FDC ID | Calories (kcal) | Fat (g) | Protein (g) | Iron (mg) |
|---|---|---|---|---|---|
| Beef, ground, 85% lean meat / 15% fat, raw | 171796 | 215 | 15.0 | 18.59 | 2.09 |
| Beef, ground, 93% lean meat / 7% fat, raw | 173110 | 152 | 7.0 | 20.85 | 2.33 |
| Lamb, domestic, leg, whole, raw | 174311 | 230 | 17.2 | 17.91 | 1.66 |
| Goat, raw | 175303 | 109 | 2.3 | 20.6 | 2.83 |

Calories/fat/protein match `src/data/macro/week-04.ts` exactly (the
point of "reconcile with the calculator" — a student reading the slide
and using the tutorial calculator sees the same numbers). Iron is new:
not previously present anywhere in this codebase, freshly sourced and
added as the deepening column per decision 8.

**One discrepancy noted, not fixed (out of this task's scope):** the
independent FDC re-check returned fat = 17.07–17.1 g for lamb leg
(174311) via two different fetch paths, while `week-04.ts` already
stores `fatPer100g: 17.2`. This is a small, pre-existing rounding
difference in a file this task was only authorized to touch "if
extending" (it wasn't extended — the four items already matched). The
deck/lecture table uses the calculator's own stored value (17.2) rather
than my independently re-verified figure (17.1), because decision 6 is
to align the slides *to the calculator*, and introducing a second,
slightly different "true" figure would recreate the exact
deck-vs-calculator mismatch this task exists to close. Flagging here for
whoever next touches `week-04.ts`.

`src/data/macro/types.ts` was not touched — the ingredient count stayed
at 4, so the 4–6 range needed no change.

## Rewritten spec: line and description (src/content/lectures/week-04.md)

**description**, before: "...plus the fat, protein and calorie content of
a mince and a ground lamb that plausibly recur in this course."

**description**, after:
> Beef as the primary material, goat and lamb touched on briefly — the
> same anatomy-then-handling-then-portioning structure as poultry and
> fish, plus the fat, protein, calorie and iron content of the two
> ground-beef blends, the lamb leg and the goat that this week's
> calculator actually uses.

**spec: line**, before: "you can state the fat, protein and calorie
content per 100 g of ground beef and ground lamb, with a source"

**spec: line**, after:
> you can state the fat, protein, calorie and iron content per 100 g of
> at least one of this week's four calculator items — two ground-beef
> blends, lamb leg and goat — with a source

Both are independently checkable: the lecture's own table plus the four
FoodData Central citation links in `links[]` let a reader verify every
figure named, and the calculator itself (linked from the tutorial
session, not this lecture) is where "at least one of this week's four
calculator items" resolves.

## Cooking-verb regex — confirmed passing

Ran the exact regex from `spec/course-promises.test.ts`
(`COOKING_PATTERN`) against `title` + `description` + `spec[]` joined,
reproducing the test's own logic in a standalone Node script:

```
match: null
```

No match, confirming the rewritten `description` and `spec:` line (and
the three untouched `spec:` lines) are all clear of every banned verb
and its inflections.

## "FoodData Central" literal string — confirmed present

All four new `links[].label` entries contain the literal string
"FoodData Central" (e.g. `USDA FoodData Central, "Beef, ground, 85% lean
meat / 15% fat, raw" (FDC ID 171796): ...`), well beyond the required
minimum of one.

## Deck changes (src/decks/week-04.deck.mdx)

- Banner retitled from "# Nutrition: ground beef and ground lamb" to
  "# Nutrition: two beef blends, lamb and goat".
- Split into **two** nutrition table slides from the outset (per the
  task's explicit instruction, and week 2's lesson about a widened table
  overflowing a single slide): "## Two ground-beef blends, per 100 g"
  (2 rows) and "## Lamb leg and goat, per 100 g" (2 rows), each with a
  6-column table (Cut, Calories, Fat, Protein, Iron, FDC ID) — the same
  column count and per-slide row count week 2's finished, non-clipping
  vegetable-nutrition slides already use as precedent.
- "## What the numbers say" rewritten to talk about the actual four
  items and the iron/fat relationship (goat leanest but most iron-dense;
  lamb leg fattest but least iron), replacing the old two-item "the two
  are close enough" text.
- Closing line fixed: "Session 4's tutorial exercises the macro
  calculator on ground beef and ground lamb" (half false against the
  real four items) → "...on two ground-beef blends, lamb leg and goat"
  (true, matches the tutorial session's own description and the
  calculator's actual contents).
- Title-image credit line and `SOURCES.md` left untouched, per decision
  12 — not part of this task's scope and no test scans it.
- `src/content/sessions/04-red-meat.mdx` left untouched, as instructed.
- `related:` edges on the lecture unchanged (still the existing two:
  `sessions/04-red-meat` and `lectures/week-03`).

## Visual verification (1280x800, dev server on port 4332)

Used Playwright (Chromium) against the running `astro dev` daemon for
this worktree (`http://localhost:4332/comp4020-ass2-hadissuryaalamin/decks/week-04/`),
`pageerror`/console-error listeners attached for every navigation:

- Walked all 22 top-level `.reveal .slides > section` elements via
  `#/<n>` hash routing. **Zero pageerror/console errors across the
  entire deck.**
- Slide "Two ground-beef blends, per 100 g": `scrollHeight` ===
  `clientHeight` === 720px (no clipping), table fully visible with room
  to spare below it, screenshotted and visually confirmed.
- Slide "Lamb leg and goat, per 100 g": same — 720px === 720px, no
  clipping, table and intro paragraph both fully visible, screenshotted
  and visually confirmed.
- "What the numbers say" slide (rewritten bullets): 720px === 720px, all
  three bullets visible with margin to spare, screenshotted.
- Final "Reading" slide (containing the fixed closing line): reached via
  keyboard `End`, confirmed 720px === 720px and the corrected sentence
  ("...on two ground-beef blends, lamb leg and goat...") renders intact,
  screenshotted.
- Neither new nutrition slide contains an `<img>` (they are table-only),
  so no `naturalWidth` check applies to them specifically; the
  whole-deck walk's zero-error result covers every image-bearing slide
  elsewhere in the deck, none of which this task touched.
- Screenshot files and the throwaway Playwright scripts used to produce
  them were written under a local `scripts-tmp/` directory and deleted
  after verification — not part of this task's file scope and not
  committed.

## Scope discipline

Touched only `src/decks/week-04.deck.mdx`,
`src/content/lectures/week-04.md`, and this task's own files
(`009-week4-nutrition.md`, this update). `src/data/macro/week-04.ts` and
`types.ts` were read but not modified (no extension was needed).
`week-03*` files and `src/decks/theme.css` were not touched — confirmed
via `git status --porcelain` before each commit that only my intended
path was staged, despite a sibling lane concurrently committing its own
week-03 work in this same shared worktree during this task.

## Status

`009-week4-nutrition.md` flipped `pending` → `in_progress` → `completed`.
