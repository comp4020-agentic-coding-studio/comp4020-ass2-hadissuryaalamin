# Task 004 — week 6 (lane B, step 2): plastic-board conversion and board-material comparison

- `plastic-cutting-board.jpg` converted from `![bg right:32% contain]` to
  an in-flow `<img>`, moved after the "## Plastic" slide's body text,
  initially styled the same as week 5's converted images (`max-height:
  220px`). Screenshot verification in task 005 found this ran 35px past
  the 720px canvas bottom on the "Plastic" slide — again a defect outside
  what `pnpm check`'s text-only overflow measurement can catch. Fixed by
  reducing to `max-height: 170px; margin: 4px auto 0;`; re-verified
  clean. See `updates/005.md` for the full verification account.
- **Flag addressed via honest alt text, per the task's own instruction not
  to silently decide the re-source question.** The photo actually shows
  strawberries being sliced in the foreground, with a white plastic board
  and a knife visible in the background — no knife scarring is visible in
  frame. Alt text written to describe what is actually shown ("A white
  plastic cutting board and a kitchen knife on a countertop, with
  strawberries being sliced in the foreground") rather than asserting the
  slide's own scarring claim is illustrated by this photo. Whether to
  replace the asset with a better-scored-board photo is left as an open
  sourcing question, not decided in this task, per scope.
- **Board-material three-way comparison: skipped, per the epic's own
  fallback ladder (option 3).** Re-confirmed the negative Commons search
  from epic decomposition still holds: `Category:Cutting_boards` has 10
  subcategories (by-country, bread, cheeseboards, fish, vegetables,
  bendable, butcher blocks, with-food, PVC, wooden) and none for marble or
  stone; direct `intitle:` searches for "marble board"/"marble slab"
  return only architectural, furniture, memorial-marble and one
  ice-cream-chain logo result, plus one fudge-on-marble-slab photo — none
  a usable kitchen cutting/pastry-board photo. `plastic-cutting-board.jpg`
  remains the deck's only board-material photo (status quo), and
  `.compare` is not invoked anywhere in either deck (grep-confirmed).
- `src/decks/assets/week-06/SOURCES.md` already carries one credit line
  per file present on disk (`wood-cutting-board.jpg`,
  `plastic-cutting-board.jpg`) — 1:1, no new asset files added.

Status: **completed**.
