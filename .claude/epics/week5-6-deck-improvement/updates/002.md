# Task 002 — week 5 (lane A): garlic/oil conversion and chopped-crushed-sliced imagery

- `garlic.jpg` converted from `![bg right:32% contain]` to an in-flow
  `<img>`, moved after the "What intact tissue keeps apart" slide's body
  text. Alt text written from what the photo actually shows: "Two garlic
  bulbs beside a peeled ivory clove and an unpeeled purple-skinned clove,
  showing the layers a cut or crush would rupture."
- `olive-oil.jpg` converted the same way, after the "How it degrades with
  use" slide's body text. Alt text: "A bottle of olive oil standing beside
  a small bowl of poured oil" — describes the photo neutrally, does not
  promote or dodge the visible brand.
- Both images initially styled `max-height: 220px; width: auto; margin: 10px
  auto; display: block;`. Screenshot verification in task 005 found
  `garlic.jpg` ran 143px past the canvas bottom at that height (720px
  canvas, "What intact tissue keeps apart" slide) — a defect `pnpm check`
  cannot see (astromotion's own `deck-check.mjs` deliberately only
  measures text-bearing elements for overflow, not images/SVGs). Fixed by
  reducing to `max-height: 75px; margin: 4px auto 0;` after an
  intermediate `90px` attempt still overflowed by 7px; re-verified clean.
  `olive-oil.jpg` had no clipping issue at `220px` and was left unchanged.
  See `updates/005.md` for the full verification account.
- **Chopped/crushed/sliced imagery: skipped, per the epic's own sanctioned
  outcome.** The carried-forward research note (task file) already
  disqualified every Commons candidate that turned up:
  - "Chopped Garlic (8364571106).jpg" — actually a Zuppa Toscana photo per
    its own Commons description, not garlic.
  - "Fresh chopped garlic on wooden board.jpg" — real subject, but
    Commons itself flags it underexposed.
  - "Ground-Garlic ... (Garlic powder)" — wrong subject (dried powder, not
    fresh cut).
  - Two genuine crushed-garlic photos exist (same photographer, same CC BY
    2.0 series) but no matching chopped/sliced entry in the same series —
    not a matched three-state set.
  - No fully vetted three-way candidate exists to the series' own
    standard. Forcing a mismatched or underexposed substitute would be
    worse than the text-only table already on the slide. Skip recorded as
    the correct, epic-sanctioned outcome, not a shortcut.
- Leaf-aromatics section: left image-free per the epic's own explicit
  lower-priority judgment call — no source search was attempted, per
  scope.
- `src/decks/assets/week-05/SOURCES.md` already carries one credit line
  per file present on disk (`fresh-herbs.jpg`, `garlic.jpg`,
  `olive-oil.jpg`) — 1:1, no new entries needed since no new asset files
  were added this task.

Status: **completed**.
