# Task 001 — setup and background-image path fix (Phase 0)

- `pnpm install` ran clean in this worktree (fresh `node_modules`/`dist`).
  Windows `.githooks` path warning appeared (`git config core.hooksPath` —
  "system cannot find path specified") but is the known-harmless warning
  seen in every worktree this session; `git status --short` confirmed no
  phantom diff afterward.
- Confirmed via direct file read (not re-screenshotted, since the harness's
  worktree-isolation bug meant no build agent this session ever obtained
  real write access to this worktree to run a dev server) that both decks'
  five `![bg …]` directives used deck-source-relative paths
  (`./assets/week-0N/...`), the same broken form documented and fixed in
  every prior deck-improvement epic this session (weeks 1-4 pattern).
- Fixed all five directives to the build-relative form
  (`../../src/decks/assets/week-0N/<file>`):
  - `week-05.deck.mdx` line 8 (`fresh-herbs.jpg`, title bg)
  - `week-05.deck.mdx` line 50 (`garlic.jpg`, right-panel bg — later
    converted to `<img>` by task 002, superseding this fix at that line)
  - `week-05.deck.mdx` line 174 (`olive-oil.jpg`, right-panel bg — later
    converted to `<img>` by task 002)
  - `week-06.deck.mdx` line 8 (`wood-cutting-board.jpg`, title bg)
  - `week-06.deck.mdx` line 79 (`plastic-cutting-board.jpg`, right-panel
    bg — later converted to `<img>` by task 004)
- Baseline `pnpm check` deferred to task 005 (the epic's sole verification
  owner), per this task's own scope note that neither lane runs
  build/check itself.
- Did not touch decks 1-4, 7-12, or the pinned astromotion version.

Status: **completed**.
