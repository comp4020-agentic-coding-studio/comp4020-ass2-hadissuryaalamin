# Task 012 — progress

First dispatch of this task crashed mid-work on an API connection error
after converting the 7 tutorial-week sessions to `.mdx` with `tutorial: true`
and a calculator embed each, but before touching the 5 no-tutorial weeks.
Picked up the remaining work directly in the coordinating session rather
than redispatching a fresh sub-agent, since the remaining scope was small
and well-defined.

## What was done

7 tutorial weeks (already done by the crashed run, verified correct):
- `02-vegetables.mdx`, `03-poultry-and-fish.mdx`, `04-red-meat.mdx`,
  `09-fruit.mdx` — `tutorial: true`, embed `MacroCalculator` with
  `src/data/macro/week-0N`.
- `05-aromatics-and-oil.mdx`, `08-timed-mise-en-place.mdx`,
  `10-food-safety-and-the-cold-chain.mdx` — `tutorial: true`, embed
  `StorageCalculator` with `src/data/storage/week-0N`.

5 no-tutorial weeks (completed this pass):
- `01-getting-started.md` already had `tutorial: false` + rationale from the
  crashed run.
- `06-kitchen-tools-and-surfaces.md`, `07-the-knife.md`, `11-first-heat.md`,
  `12-the-live-test.md` — added `tutorial: false` to frontmatter plus a short
  "Why there is no calculator this week" / "Why there is no tutorial"
  section each, explaining why that week's existing exercise (or absence of
  one) isn't a calculator-based tutorial.

## Fix during verification

`pnpm check`'s course-promises spec test failed: week 10's description/spec
used the word "cooked" (raw vs. cooked leftover), which the round-1 "never
claims cooking before week 11" test flags regardless of context. Reworded
to "already-prepared leftover" / "reheated to a safe temperature" — same
meaning, no banned cooking verb, since the storage exercise is about a
safe-until clock, not cooking itself.

## Verification

`pnpm check` green: typecheck 0 errors, build 48 pages / 0 axe violations /
no broken links / 12 decks structurally valid, vitest 6/6 (all of round 1's
five plus the twelve-weeks-numbered test).

Sessions collection still has exactly 12 entries. Exactly 5 carry
`tutorial: false` (1, 6, 7, 11, 12) and exactly 7 carry `tutorial: true`
(2, 3, 4, 5, 8, 9, 10).

Status: completed.
