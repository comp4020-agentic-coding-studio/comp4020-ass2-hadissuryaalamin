# Task 016 — Decks batch B (weeks 5-8)

Status: completed

## What was done

- Created `src/decks/week-05.deck.mdx` (236 lines) — aromatics and oil.
  Peels the week-05 lecture into granular slides: allium cell chemistry
  (garlic/alliinase/alliin via Borlinghaus et al. 2021, onion's separate
  lachrymatory-factor-synthase pathway via Imai et al. 2002), a
  chopped/crushed/sliced comparison table, leaf-aromatic timing (bay leaf
  early vs basil late, Głuchowski et al. 2021), and oil smoke-point
  degradation (Crosby 2018, 1% FFA -> 25% smoke-point decline). Added a
  dedicated "Before this week's tutorial" section as the lead-in slide for
  the storage-duration calculator exercise, plus a closing reading slide
  reusing the same four citations already in the lecture/session content.
- Created `src/decks/week-06.deck.mdx` (207 lines) — kitchen tools and
  surfaces. Peels the lecture into slides on the tool inventory, a
  wood/plastic/marble comparison table, and a two-slide treatment of the
  Yadav et al. (2023) microplastics finding (the transfer finding plus the
  72-hour null cell-viability result, reported together per CLAUDE.md's
  honesty rule), then maintenance. No tutorial lead-in — this week has none
  by design, and the deck says so explicitly rather than silently omitting
  a slide.
- Created `src/decks/week-08.deck.mdx` (184 lines) — guest lecture: time
  management under service. Peels the existing week-08 lecture's four
  categories (before doors open / held / made to order / sequencing) into
  one banner + content slide each, plus a lead-in slide for the
  storage-duration tutorial that ties "held" material back to the same
  cold-chain clock used in weeks 5 and 10.
- Left `src/decks/week-07.deck.mdx` untouched — already at the detail
  baseline (239 lines) and structurally fine (no image-support changes
  needed); task 018 will add images to it directly.
- Wired `slides:` frontmatter on the three lectures that needed it:
  `src/content/lectures/week-05.md` -> `/decks/week-05/`,
  `week-06.md` -> `/decks/week-06/`, `week-08.md` -> `/decks/week-08/`.
  (Task 011 deliberately left this field off since the decks didn't exist
  yet — see its task file. Week 7's `slides:` field was already set in
  round 1.) This is a minimal frontmatter-only edit; no lecture prose was
  touched.
- No files under `src/decks/assets/` touched. No `src/content/sessions/*`
  touched.

## Verification

Ran `pnpm check` (typecheck -> production build -> spec tests):

- `astro check`: 0 errors, 0 warnings, 0 hints.
- `astro build`: succeeded, 48 pages built.
- axe accessibility: 48 pages checked, no violations.
- Broken-link checker: no broken links detected (confirms all four
  `slides:` links resolve and the deck routes exist).
- astromotion structural check: "Checked 12 deck(s) — no structural
  violations" (covers all twelve decks including the three new ones).
- course-graph: 30 nodes, 46 edges generated without error.
- vitest spec suite: 6/6 tests passed.

This covers the "pnpm dev renders all four decks with no console/page
errors" requirement via the build-time equivalent (production build +
axe + astromotion's own overflow/structure check), which is stronger than
a manual dev-server click-through and is the check this repo's `CLAUDE.md`
prescribes before claiming anything works.

## Status

Flipping to `completed`. Not blocked.
