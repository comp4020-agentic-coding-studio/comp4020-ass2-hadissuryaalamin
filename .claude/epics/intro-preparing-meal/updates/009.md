# Task 009 — Spec tests

Wrote `spec/course-promises.test.ts` (sibling to `spec/data-integrity.test.ts`,
which was left untouched) reading `dist/api/index.json`, same pattern as the
existing test.

Five tests:
1. Assessment weights sum to exactly 100; each `weighted` marking scheme's
   criteria also sum to 100.
2. Sessions cover weeks 1-12 exactly, no gaps/duplicates.
3. No session/lecture in weeks 1-10 matches a cooking-verb/noun pattern in
   title, description, or `spec:` lines.
4. Every node in a graph collection (`sessions`, `assessments`, `lectures`,
   `people` — matches `graphCollections` in `site-config.ts`; `policies` is
   deliberately excluded there, so excluded here) has ≥1 `related` edge.
5. At least one lecture has a non-empty `meta.slides`.

Verified all five pass against the actual built content from tasks 001-008
— no content bugs found, nothing needed fixing. `pnpm check` (typecheck +
build + vitest) passes end to end: 0 typecheck errors, 30 pages built with
no accessibility violations and no broken links, 6/6 tests green.

Status: completed.
