# Task 017 — Decks batch C (weeks 9-12)

Status: completed.

## What was done

Created four new decks at the week-07 detail baseline:

- `src/decks/week-09.deck.mdx` (229 lines) — Fruit. Climacteric vs
  non-climacteric split, storage (counter-only / counter-then-fridge /
  fridge-from-the-start), the ethylene link back to week 2, market
  selection, ripening as a schedulable process, and a dedicated nutrition
  section (apple/banana/orange fat/protein/calories per 100 g, USDA
  FoodData Central) feeding this week's macro-calculator tutorial.
- `src/decks/week-10.deck.mdx` (198 lines) — Food safety and the cold
  chain. Danger zone (4-60 °C, doubling time, two-hour rule, a worked
  example), the clean/separate/cook/chill framework, a holding-time table
  synthesising weeks 3/4/9 plus a vegetables/week-2 storage-clock slide,
  the "coldest link" synthesis argument, and a clear lead-in slide to this
  week's storage-duration calculator tutorial.
- `src/decks/week-11.deck.mdx` (174 lines) — Cooking (first cook week, no
  tutorial). The two-batch comparison design, what variable can be
  deviated, thermometer-checked doneness against sourced minimum internal
  temperatures, why colour/time are unreliable, what is compared and how
  a null result is still recorded, forward link to week 12.
- `src/decks/week-12.deck.mdx` (157 lines) — Live test: the public
  kitchen (no tutorial). What is examined (preparation, not the plate
  alone), how the exam reads the plate, the one new condition (fixed
  time + audience, no rehearsal), correspondence with the week 6
  invent-a-recipe account, and the closing note that this is the last
  teaching week.

File scope respected: only the four `week-0N/1N.deck.mdx` files under
`src/decks/` were touched. Weeks 1-8 decks, `src/decks/assets/`,
`src/content/lectures/*` prose and `src/content/sessions/*` were left
alone except for the one frontmatter field below.

## Wiring

Set `slides:` on the four matching lecture pages (`src/content/lectures/
week-09.md`, `week-10.md`, `week-11.md`, `week-12.md`) to `/decks/week-09/`,
`/decks/week-10/`, `/decks/week-11/`, `/decks/week-12/` respectively — the
only edit made outside `src/decks/`.

## Verification

`pnpm check` (typecheck → build → spec tests) is green:

- `astro check`: 0 errors, 0 warnings, 0 hints (37 files).
- `astro build`: 48 pages built; astro-theme-university a11y check — 0
  violations across 48 pages; broken-links checker — no broken links;
  astromotion's own structural check — "Checked 12 deck(s) — no
  structural violations" (all twelve decks, not just these four).
- `vitest run spec`: 6 tests passed (2 files).

Also started `pnpm dev` and curled all four new deck routes
(`/decks/week-09/` through `/decks/week-12/`) — all returned HTTP 200,
dev server log shows no errors.

No blockers. Status flipped to `completed`.
