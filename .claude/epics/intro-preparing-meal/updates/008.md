# 008 — home page, policies page, artwork, session labels

Status: completed.

## Home page (`src/pages/index.astro`)

- Removed both `STARTER_CONTENT` markers.
- Dropped the hero image deliberately: no `heroImage`/`heroImageAlt` props are
  passed to `ContentLayout` any more. `ContentLayout.astro` (in
  `astro-theme-university`) already handles the absent-hero case by rendering
  a plain `<h1>{title}</h1>` instead, so this is a supported, non-hacky
  image-free treatment rather than a broken prop.
- The thesis ("a meal is decided before it is cooked") is stated in the first
  screen via `courseMeta.description`, which is rendered as the lead
  paragraph directly under the H1. Left `course-config.ts` untouched per the
  file-scope rule, and did not restate the description's wording in the new
  body prose — wrote new "What you will do" / "Who it is for" copy instead.
- Updated the "Where to go next" card copy to read `each {sessionLabels.singular.toLowerCase()}`
  so it tracks the renamed session label instead of hardcoding "teaching
  session".

## Policies page (`src/pages/policies/index.mdx`)

- Already had `layout: ../../layouts/PageLayout.astro` frontmatter from the
  starter — confirmed it's correct per the Windows `defaultLayout` fix
  (commit `f1c1ef8`) and left it in place.
- Replaced the placeholder body with six sections: Late work, Extensions,
  Academic integrity, Allergies and dietary requirements, Kitchen safety, and
  Missing the week 12 live test.
- Cross-checked the assessment-weight figure used in the last section (60%
  for the week-12 practical) against `src/content/assessments/*.md`, which
  another task already set to 40/60 — the policies page's number matches
  rather than inventing its own.
- No fabricated regulation numbers; the late-work/extension mechanics are
  Slop University's own invented administrative policy (fair game), while the
  allergy/kitchen-safety practices described are generic good practice
  (cross-contact separation, declaring allergens, PPE) without citing any
  specific external standard or statute.
- Zero `STARTER_CONTENT` markers remain.

## Artwork (`src/assets/images/`)

`pnpm check:evidence` hashes both `card.png` and `hero-home.avif` as starter
assets that must not survive unchanged, so both needed resolving, not just the
one used as the home hero:

- `hero-home.avif` — deleted. The home page went image-free (see above); the
  evidence check explicitly treats a deleted starter image as a valid
  resolution ("a deleted file passes... so is an image-free treatment").
- `card.png` (the social-preview image referenced by `siteConfig.socialImage`
  in `site-config.ts`, outside this task's edit scope) — regenerated in place
  at the same path/dimensions (1200x630 PNG) using `sharp` (already a
  dependency) to rasterize a small flat SVG in the site's own gold/bronze/grey
  ink palette (`astro-theme-slop/slop.css`): a cutting board, a knife laid
  across it, and the course code/title/thesis as flat type. New sha256 is
  `476b750c...` vs the starter's hash in `scripts/check-evidence.ts` — content
  confirmed to differ. The generator script was scratch (written to the repo
  root, run once, deleted) and is not part of this commit.

## `sessionLabels` (`src/site-config.ts`)

- Set to `{ singular: "Prep", plural: "Preps" }` per `CLAUDE.md`. Touched only
  this value; the rest of the file (branding spread, `siteConfig`,
  `courseApiCollections`, etc.) is untouched.
- Read both existing consumers to sanity-check the wording (out of this
  task's edit scope, but worth confirming before calling this done):
  - `src/pages/sessions/[slug].astro` — title becomes e.g. "Week 3 Prep:
    Vegetables". Reads naturally.
  - `src/pages/sessions/index.astro` — page title "Preps"; description
    "...what happens in each prep, what to prepare, and what leaves the
    room." Both read naturally, and "prep" doubles nicely as the course's own
    vocabulary.
  - `src/pages/index.astro`'s own card copy, updated as above.

## Not run

Per instructions, did not run `pnpm build`/`check`/`dev` — other agents are
editing concurrently in this shared epic worktree. These files should be
covered by the epic's post-integration `pnpm check` pass.
