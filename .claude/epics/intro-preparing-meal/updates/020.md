# Task 020 — Final integration and check — round 2

Tasks 011-019 landed clean; `pnpm check` needed no fixes this round (no
YAML/MDX errors like round 1 surfaced) — typecheck, build and spec tests
were green on the first run.

## `pnpm check` output

- `astro check`: 0 errors, 0 warnings, 0 hints (37 files).
- `astro build`: 48 pages built in 4.80s. Zero accessibility violations
  (axe), zero broken internal links, deck asset copy (31 files) and deck
  structural check both clean ("Checked 12 deck(s) — no structural
  violations").
- `vitest run spec`: 2 test files, **11/11 tests passed** — round 1's five
  course-promise tests plus round 2's five (spec tests 6-10), plus the one
  data-integrity test, all green.

## Round 2 definition of done — verified against `dist/api/index.json`

- **Twelve lecture pages, weeks 1-12, no gaps**, each `slides:` resolving
  to a deck with a built page at `dist/decks/week-NN/index.html` —
  confirmed directly against the API (all 12 present, `week-01` through
  `week-12`) and by spec test "gives every lecture a slide deck that
  actually exists in the built output".
- **Twelve decks, weeks 1-12** — `dist/decks/` holds all twelve directories;
  build log shows "Generated course API: 30 nodes... " and "Checked 12
  deck(s) — no structural violations".
- **Seven tutorial pages (weeks 2, 3, 4, 5, 8, 9, 10), five no-tutorial
  pages (weeks 1, 6, 7, 11, 12), twelve sessions entries total** —
  confirmed against the API: `tutorial: true` on exactly weeks
  2,3,4,5,8,9,10 and `tutorial: false` on exactly 1,6,7,11,12. Matches
  spec test "marks exactly seven sessions entries tutorial:true... and
  five tutorial:false".
- **Macro/calorie calculator** (`src/components/MacroCalculator.astro`)
  used on weeks 2, 3, 4, 9's tutorial pages (`grep -rl MacroCalculator
  src/content/sessions/` returns exactly those four files). Weeks 2, 3, 4,
  9 lectures each cite a `fdc.nal.usda.gov/food-details/.../nutrients`
  URL with a "FoodData Central" label — confirmed both by spec test
  "cites a USDA FoodData Central source..." and by direct inspection of
  each lecture's `dist/api/<id>.json` `links[]`.
- **Storage-duration calculator**
  (`src/components/StorageCalculator.astro`) used on weeks 5, 8, 10's
  tutorial pages (`grep -rl StorageCalculator src/content/sessions/`
  returns exactly those three files). Weeks 5, 8, 10 each cite a
  USDA/FSIS storage-safety source (`fsis.usda.gov/.../danger-zone-40f-140f`,
  `.../refrigeration`, `.../leftovers-and-food-safety`) on the lecture
  and/or session page — confirmed by spec test and by direct link
  inspection.
- **Deck detail**: line counts for `src/decks/week-*.deck.mdx` range from
  161 (week 12) to 245 (week 7, the reworked baseline) lines, all
  materially above round 1's original week-01 (99 lines); every deck
  other than week-11/12 sits at 178-245 lines, in the same range as the
  week-07 baseline (239-245).
- **Deck images**: every deck's background/inline images resolve to
  `./assets/<week-slug>/...`, and `src/decks/assets/` holds a populated
  directory (with a `SOURCES.md`) for every one of weeks 01-12. The
  build's own broken-link and deck-structure checks both came back
  clean — "No broken links detected" and "no structural violations".
- **Spec tests 6-10 written and passing**, alongside round 1's five —
  all eleven tests in `spec/course-promises.test.ts` +
  `spec/data-integrity.test.ts` pass (see `pnpm check` output above).
- **`pnpm check` green.** Confirmed above.
- **Platform files unchanged vs `main`**: `git diff main --
  astro.config.ts`, `scripts/pages-base.ts` and `src/content.config.ts`
  are all empty. `src/site-config.ts`'s diff is exactly the
  `sessionLabels` singular/plural pair (`"Session"/"Sessions"` →
  `"Prep"/"Preps"`), the same authorized scope round 1 already used —
  no other line in that file differs from `main`. `SLOP1640` keeps its
  code and `courseMeta.level` stays `1`.

## Citation spot-checks

Same pattern round 1's `updates/010.md` recorded: several USDA/FSIS/
FoodSafety.gov domains return HTTP 403 to a scripted `curl`/`WebFetch`
(bot-blocking WAF), not because the page is missing.

- `https://www.fsis.usda.gov/.../danger-zone-40f-140f` and
  `https://www.foodsafety.gov/food-safety-charts/cold-food-storage-charts`
  both 403'd to a direct fetch; a web search for each surfaced the exact
  live page with matching title and content (the FSIS "Danger Zone"
  40-140°F guidance, and FoodSafety.gov's Cold Food Storage Chart) —
  genuine, not fabricated.
- `https://www.ams.usda.gov/sites/default/files/media/
  EthyleneCropsTechnicalReport2023.pdf` returned HTTP 200 directly.
- `https://fdc.nal.usda.gov/food-details/170000/nutrients` (cited in
  week 2's lecture) 404's to a direct fetch, because FDC's food-details
  page is a client-rendered SPA route a plain GET can't resolve — but
  the underlying FDC ID is real: `api.nal.usda.gov/fdc/v1/food/170000`
  (USDA's public API, `DEMO_KEY`) returned `"description":"Onions,
  raw"`, matching the ingredient it's cited for in the week 2 macro
  table. No citation found to be fabricated or dead in this round's
  sample.

## `pnpm check:evidence`

Fails only on the same three items round 1's `updates/010.md` already
deferred and this task is told not to touch:

```
✓ reflections/: none needed — an assignment's written account is PROCESS.md
✗ PROCESS.md still contains the template comment — replace the boilerplate with your own overview
✗ cited commit a1b2c3d doesn't exist in this repo
✗ cited commit e4f5a6b doesn't exist in this repo
```

`git diff main -- PROCESS.md` is empty — the file is untouched since
round 1 left it, confirming no new `check:evidence` failure was
introduced this round. `PROCESS.md` remains out of this epic's scope
(written last, from the finished commit history).

Status: completed. `pnpm check` green (typecheck, build, 11/11 spec
tests); `pnpm check:evidence` blocked only by the same deliberately
deferred `PROCESS.md` item round 1 left, nothing new.
