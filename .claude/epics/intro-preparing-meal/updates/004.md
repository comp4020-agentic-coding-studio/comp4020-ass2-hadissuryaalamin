# Task 004 — Lecture page + slide deck: week 7 (the knife)

## Status: completed

## What was done

- Read `.claude/epics/intro-preparing-meal/epic.md`, `src/content.config.ts`
  (lectures schema: `slides` must match `^/decks/[a-z0-9-]+/$`), `src/decks/theme.css`,
  and the starter `src/decks/week-01.deck.mdx` for the deck format before
  writing anything.
- Confirmed the deck route convention directly from `astro.config.ts`
  (comment on the `astromotion` integration: every `.deck.mdx` under
  `src/decks/` becomes a page at `/decks/<name>/`) and from the already-wired
  example (`week-01.md` → `slides: /decks/week-01/` → `week-01.deck.mdx`).
  So `src/decks/week-07.deck.mdx` builds to `/decks/week-07/`, matching the
  `slides:` field written on the new lecture.
- Found `src/content/people/idris-fenn.md` already exists and already
  declares itself as week 7's teacher (`description` names "the knife",
  `related: [lectures/week-07]`), so used `teachers: [idris-fenn]` on the
  new lecture rather than inventing a person.
- Created `src/content/lectures/week-07.md`: title "The knife", dated
  2027-04-05, `teachers: [idris-fenn]`, `slides: /decks/week-07/`,
  `related: [sessions/07-the-knife]` (the session slug task 002 is writing
  concurrently for week 7 — "the-knife" is the natural slug given the
  epic's own heading; task 010's final integration is the designated place
  to reconcile a slug mismatch if task 002 chose differently, per its
  checklist). Body is a short summary/outline that frames the deck as the
  primary artefact and the page as its container, per the epic. No
  cooking is claimed or implied anywhere in the file.
- Created `src/decks/week-07.deck.mdx`: the primary teaching artefact for
  the most detailed week in the course. Covers knife types and their
  purposes, blade anatomy, edge angle vs. steel hardness trade-offs (HRC
  scale, Western ~17–22°/side vs. Japanese ~10–15°/side), typical blade
  lengths (mm) and weights (g) per knife type, balance, sharpening vs.
  honing as distinct operations with a stated maintenance schedule
  (hone before session / sharpen every 4–6 weeks / inspect before
  sharpening), grip (pinch grip + guide-hand claw), and the standard cuts
  with target dimensions in mm. Cites a real, checkable source (Verhoeven,
  *Experiments on Knife Sharpening*, Iowa State University, 2004 —
  <https://archive.org/details/Experiments_on_Knife_Sharpening_John_Verhoeven>)
  for the sharpening/honing and edge-geometry material rather than dressing
  up blog-sourced numbers as research; dimension/hardness ranges are
  presented as general trade figures, not attributed to a study. Ends with
  a fictional-teacher quote (Idris Fenn, already established in this
  course) and a reading slide. No images used — text/table only, consistent
  with a deliberate image-free treatment.
- Retired the starter `src/decks/week-01.deck.mdx`: overwrote its content
  (removed the `STARTER_CONTENT` marker) rather than deleting the file,
  because `src/content/lectures/week-01.md` already links it via
  `slides: /decks/week-01/` and that lecture file is task 003's concurrent
  scope, not mine — deleting the deck would have left task 003's file with
  a dangling `slides:` ref. Replaced it with a real week-1 deck ("Why
  prepare at all") built on the same three real citations the epic already
  specifies for week 1 (USDA ERS × 2, Monsivais/Aggarwal/Drewnowski 2014),
  so the route stays a genuine artefact instead of a stub.
- Verified with `grep -rn STARTER_CONTENT src/decks/ src/content/lectures/week-07.md`
  — zero matches.
- Did not run `pnpm build`/`check`/`dev` per instructions (concurrent
  agents elsewhere in the same worktree). MDX syntax was checked by close
  reading against `week-01.deck.mdx`'s original structure (frontmatter,
  `---` slide separators, `{/* _class: ... */}` comments) rather than a
  build.

## Files touched (this task's scope only)

- `src/content/lectures/week-07.md` (new)
- `src/decks/week-07.deck.mdx` (new)
- `src/decks/week-01.deck.mdx` (rewritten, marker cleared)

## Notes for task 010 (final integration)

- Confirm `sessions/07-the-knife` (the slug I guessed for the `related:`
  edge) actually matches whatever slug task 002 gave
  `src/content/sessions/07-*.md`; fix the ref if it differs — `pnpm check`
  will throw a clear `dangling-ref` error naming both sides if so.
- `pnpm check`/`pnpm build` not yet run against these files by this task.
