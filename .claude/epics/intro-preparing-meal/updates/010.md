# Task 010 — Final integration and check

First full `pnpm build` after tasks 001-008 landed surfaced three real
build errors from the concurrent batch (fixed in commit `7cca534`, before
this task started):

- `src/content/lectures/week-08.md`: unquoted `title:` containing a bare
  `": "` mid-value (`Guest lecture: time management...`) — js-yaml read it
  as a nested mapping key. Quoted.
- `src/content/sessions/11-first-heat.md`: same bug in a `links[].label`
  value (`USDA, "Cooking Meat: Is It Done Yet?"`). Quoted.
- `src/decks/week-01.deck.mdx` and `week-07.deck.mdx`: bare `<https://...>`
  autolinks, which MDX parses as JSX and rejects on the `/`. Converted to
  `[text](url)`.
- `src/pages/404.md` still pointed `heroImage` at the hero art task 008
  deliberately deleted, so it warned every build with an unresolved asset
  path (not fatal, but real). Dropped the two hero fields — the 404 page
  didn't need one anyway.

## Checklist verification (epic.md "Definition of done")

- Twelve session entries, dated Mondays 2027-02-22 through 2027-05-10, no
  gaps — confirmed via the built API (`dist/api/index.json` weeks 1-12,
  sorted, no duplicates) and by spec test 2 (task 009).
- Lecture pages for weeks 1, 3, 7, 8 exist; week 7's lecture links the
  deck via `slides: /decks/week-07/` — confirmed in the API and by spec
  test 5.
- Two assessments (`invent-a-recipe` 40, `the-public-kitchen` 60) sum to
  exactly 100; each has a `weighted` marking block that itself sums to
  100 — confirmed via the API and spec test 1.
- People: `marisol-quaye` (convenor), `idris-fenn` (teacher), both
  starters rewritten; `dominic-achterberg` (guest lecturer) is wholly
  invented, no real chef named or quoted.
- Home page states the course thesis ("A meal is decided before it is
  cooked.") in `courseMeta.description`, rendered as the `ContentLayout`
  description on the first screen. Policies page covers late work,
  extensions, academic integrity, allergies/dietary, kitchen safety, and
  live-test non-attendance, with explicit `layout:` frontmatter. Starter
  hero art and card image replaced/dropped (`src/assets/images/` now
  holds only the regenerated `card.png`; homepage is deliberately
  image-free).
- `grep -rl STARTER_CONTENT src/` returns nothing.
- The five spec tests from task 009 pass.
- `pnpm check` (typecheck + build + vitest) exits clean: 0 typecheck
  errors, 30 pages built with 0 accessibility violations and 0 broken
  links, 6/6 tests green.
- `pnpm check:evidence` fails only on the two items task 010 is explicitly
  told to leave alone: `PROCESS.md`'s unfilled template comment and its
  two placeholder commit hashes (`a1b2c3d`, `e4f5a6b`). Per this task's own
  constraints, `PROCESS.md` is out of this epic's scope — written last,
  separately, once the commit history exists to cite. No other
  check:evidence failures.
- Spot-checked a broad sample of cited URLs (USDA ERS, FSIS, FDA, ACS,
  ScienceDirect, NDSU/UMD extension, Nature, Archive.org, Wikipedia).
  Several USDA/ACS/Elsevier domains return HTTP 403 to a scripted
  `curl`/fetch (bot-blocking WAFs — confirmed for two representative
  cases via web search, which surfaced the exact page title and content
  at the exact cited URL, including that the ACS microplastics study's
  reported null result on 72h mouse-fibroblast viability is accurately
  and honestly represented in `06-kitchen-tools-and-surfaces.md`). No
  citation found to be fabricated or dead.
- Platform files confirmed untouched vs `main`: `astro.config.ts`,
  `scripts/pages-base.ts`, `src/content.config.ts` show no diff.
  `src/site-config.ts` diff is exactly the two `sessionLabels` lines
  (task 008's authorized scope). Course code remains `SLOP1640` (last
  three digits `640` unchanged).

Status: completed. `pnpm check` green; `pnpm check:evidence` blocked only
by the deliberately-deferred `PROCESS.md`.
